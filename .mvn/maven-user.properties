# IKE Workspace: project-local installed artifact repository
# Ensures branch isolation when multiple workspaces are active.
# See ike-workspace-conventions.adoc for rationale.
maven.repo.local.path.installed=${session.rootDirectory}/.mvn/local-repo
