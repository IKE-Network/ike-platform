<?xml version="1.0" encoding="UTF-8"?>
<!--
  IKE Network — Parent POM
  ═══════════════════════════

  The standard parent for all IKE Network projects. Inheriting this
  POM provides two things through a single <parent> declaration:

    1. Build conventions: Java 25 compiler configuration, ASM 9.9 for
       annotation processing, surefire/failsafe with JPMS add-opens,
       GPG signing with Bouncy Castle, platform classifier profiles,
       enforcer rules, and the AsciiDoc documentation pipeline (activated
       when src/docs/asciidoc exists). Inherited from the ike-platform
       reactor root.

    2. Dependency version management — centralized versions for IKE
       infrastructure artifacts (ike-docs, ike-tooling), AsciiDoc
       toolchain dependencies, and shared test dependencies (JUnit 5,
       AssertJ, Mockito). Declared inline using properties inherited
       from the root POM.

    3. Extension-true declarations for ike-doc-maven-plugin and
       ike-maven-plugin. These are LITERAL versions (not ${property})
       because Maven resolves extension plugin JARs at project-load
       time, before property interpolation.

  Usage (inheritance — recommended):

    <parent>
        <groupId>network.ike.platform</groupId>
        <artifactId>ike-parent</artifactId>
        <version>1</version>
    </parent>

  Dependency versions are declared inline (no BOM import) to avoid
  Maven 4's "BOM imports from within reactor" warning. Version
  properties are inherited from the root POM (single source of truth).
-->
<project xmlns="http://maven.apache.org/POM/4.1.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.1.0
         http://maven.apache.org/xsd/maven-4.1.0.xsd">
    <modelVersion>4.1.0</modelVersion>

    <parent>
        <groupId>network.ike.platform</groupId>
        <artifactId>ike-platform</artifactId>
    </parent>

    <artifactId>ike-parent</artifactId>
    <packaging>pom</packaging>

    <name>IKE Parent</name>
    <description>
        Standard parent POM for IKE Network projects. Inheriting this
        POM provides build conventions (Java 25 compiler, test harness,
        GPG signing, AsciiDoc documentation pipeline) from the
        ike-platform reactor root and centralized dependency version
        management declared inline. Declares ike-doc-maven-plugin
        (from network.ike.docs) with extensions=true to provide the
        ike-doc custom packaging type to external doc projects.
    </description>
    <url>https://ike.network/ike-platform/ike-parent/</url>
    <inceptionYear>2026</inceptionYear>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <organization>
        <name>IKE Network</name>
        <url>https://ike.network</url>
    </organization>

    <scm>
        <connection>scm:git:https://github.com/IKE-Network/ike-platform.git</connection>
        <developerConnection>scm:git:git@github.com:IKE-Network/ike-platform.git</developerConnection>
        <url>https://github.com/IKE-Network/ike-platform</url>
        <tag>HEAD</tag>
    </scm>

    <distributionManagement>
        <repository>
            <!-- Developers deploy releases to ike-staging. -->
            <!-- TeamCity verifies reproducibility then promotes to ike-releases. -->
            <id>ike-staging</id>
            <url>${deploy.staging.url}</url>
        </repository>
        <snapshotRepository>
            <id>ike-snapshots</id>
            <url>${deploy.snapshot.url}</url>
        </snapshotRepository>
        <!-- <site> URL for site:stage relative-path computation.
             Set to ike-parent's actual gh-pages publish location
             (inside ike-platform/'s gh-pages tree), so the Modules
             sidebar and parent-project links computed by Maven Site
             resolve correctly when content is published.

             ike-issues#380: previously this URL was
             https://ike.network/${project.artifactId}/ — a template
             tuned for external CONSUMERS (doc-example, example-project,
             ike-example-its, ike-example-ws). For ike-parent itself
             (published at https://ike.network/ike-platform/ike-parent/)
             that template was wrong, and produced ../../ike-parent/
             relative paths in ike-platform's Modules sidebar that
             resolved to 404 on the un-versioned root publish.

             Consumers must now declare their own <distributionManagement>
             <site> URLs (doc-example and example-project already did;
             ike-example-its and ike-example-ws were updated under #380).

             child.site.url.inherit.append.path="false" disables
             Maven's automatic append-child-artifactId-to-inherited-URL
             rule, preventing ${project.artifactId}/${project.artifactId}/
             doubling in any child that does still inherit (ike-issues#358).

             The actual content publish still goes through
             publishProjectSiteToGhPages (#304: scpexe deploy retired). -->
        <site child.site.url.inherit.append.path="false">
            <id>ike-site</id>
            <url>https://ike.network/ike-platform/${project.artifactId}/</url>
        </site>
    </distributionManagement>

    <!-- ════════════════════════════════════════════════════════════════ -->
    <!-- Dependency Management — centralized versions for the IKE         -->
    <!-- ecosystem. Version properties are inherited from the root POM.   -->
    <!-- External consumers inheriting this POM get build conventions     -->
    <!-- (from root) + dependency versions (from here).                   -->
    <!-- ════════════════════════════════════════════════════════════════ -->

    <!-- Top-level properties — visible to external consumers via         -->
    <!-- standard Maven inheritance. Java version + plugin versions       -->
    <!-- needed by the pluginManagement compiler/surefire/failsafe        -->
    <!-- configs below; without them external consumers wouldn't get      -->
    <!-- preview features enabled (see #280).                             -->
    <properties>
        <java.version>25</java.version>
        <maven-compiler-plugin.version>3.14.0</maven-compiler-plugin.version>
        <maven-surefire-plugin.version>3.5.2</maven-surefire-plugin.version>
        <maven-failsafe-plugin.version>3.5.2</maven-failsafe-plugin.version>
        <maven-jar-plugin.version>3.4.2</maven-jar-plugin.version>
        <maven-source-plugin.version>3.3.1</maven-source-plugin.version>
        <jacoco-maven-plugin.version>0.8.14</jacoco-maven-plugin.version>
        <!-- ASM in surefire/failsafe/compiler plugin realm — release that
             supports classfile major 69 (Java 25). See #282 (surefire/
             failsafe) and #283 (compiler). -->
        <asm.version>9.9.1</asm.version>
        <!-- Record-builder annotation processor — used across IKE Java
             modules. See #282. -->
        <record-builder-core.version>34</record-builder-core.version>
        <!-- Skip jacoco with -Dike.skip.jacoco=true. -->
        <ike.skip.jacoco>false</ike.skip.jacoco>
        <!-- Unified logging (ike-issues#936): SLF4J API + Log4j2 binding, the
             same strategy tinkar-core and komet run. Both lines are JPMS-
             compliant (log4j-core / log4j-slf4j2-impl and slf4j-api 2.0.x ship
             module-info), so a jlinked runtime stays module-path clean.
             Versions are managed via slf4j-bom / log4j-bom below. -->
        <slf4j.version>2.0.17</slf4j.version>
        <log4j.version>2.26.0</log4j.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- ── Unified logging (ike-issues#936) ───────────────────── -->
            <!-- SLF4J API + Log4j2 binding, managed via their BOMs so every
                 inheriting module resolves the same versions. -->
            <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>slf4j-bom</artifactId>
                <version>${slf4j.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.apache.logging.log4j</groupId>
                <artifactId>log4j-bom</artifactId>
                <version>${log4j.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- ── IKE Infrastructure Artifacts ───────────────────────── -->

            <!-- Claude instruction files (classified ZIP) -->
            <dependency>
                <groupId>network.ike.tooling</groupId>
                <artifactId>ike-build-standards</artifactId>
                <version>${network.ike.tooling__GA__ike-tooling__VERSION}</version>
                <classifier>claude</classifier>
                <type>zip</type>
            </dependency>

            <!-- Human-readable convention documents (classified ZIP) -->
            <dependency>
                <groupId>network.ike.tooling</groupId>
                <artifactId>ike-build-standards</artifactId>
                <version>${network.ike.tooling__GA__ike-tooling__VERSION}</version>
                <classifier>docs</classifier>
                <type>zip</type>
            </dependency>

            <!-- .asciidoctorconfig for IDE preview (classified ZIP) -->
            <dependency>
                <groupId>network.ike.tooling</groupId>
                <artifactId>ike-build-standards</artifactId>
                <version>${network.ike.tooling__GA__ike-tooling__VERSION}</version>
                <classifier>asciidoctorconfig</classifier>
                <type>zip</type>
            </dependency>

            <!-- Shared build config files (editorconfig, checkstyle, stignore) -->
            <dependency>
                <groupId>network.ike.tooling</groupId>
                <artifactId>ike-build-standards</artifactId>
                <version>${network.ike.tooling__GA__ike-tooling__VERSION}</version>
                <classifier>config</classifier>
                <type>zip</type>
            </dependency>

            <!-- Forest theme site.css + ike-logo.svg (classified ZIP) — see #318 -->
            <dependency>
                <groupId>network.ike.tooling</groupId>
                <artifactId>ike-build-standards</artifactId>
                <version>${network.ike.tooling__GA__ike-tooling__VERSION}</version>
                <classifier>site-theme</classifier>
                <type>zip</type>
            </dependency>

            <!-- Platform-wide built-with supplement (classified ZIP) — see #340 -->
            <dependency>
                <groupId>network.ike.tooling</groupId>
                <artifactId>ike-build-standards</artifactId>
                <version>${network.ike.tooling__GA__ike-tooling__VERSION}</version>
                <classifier>built-with</classifier>
                <type>zip</type>
            </dependency>

            <!-- Scaffold templates (classified ZIP) consumed by
                 ike:scaffold-{draft,publish,revert} via the
                 unpack-scaffold-templates execution wired into
                 ike-parent (#243). -->
            <dependency>
                <groupId>network.ike.tooling</groupId>
                <artifactId>ike-build-standards</artifactId>
                <version>${network.ike.tooling__GA__ike-tooling__VERSION}</version>
                <classifier>scaffold</classifier>
                <type>zip</type>
            </dependency>

            <!-- Documentation resources: themes, assembly descriptors, configs
                 (external artifact — lives in ike-docs repo) -->
            <dependency>
                <groupId>network.ike.docs</groupId>
                <artifactId>ike-doc-resources</artifactId>
                <version>${network.ike.docs__GA__ike-docs__VERSION}</version>
            </dependency>

            <!-- Noto fonts for PDF generation (minimal subset) -->
            <dependency>
                <groupId>network.ike.docs</groupId>
                <artifactId>minimal-fonts</artifactId>
                <version>${network.ike.docs__GA__ike-docs__VERSION}</version>
                <type>zip</type>
            </dependency>

            <!-- DocBook XSL 1.79.2 + IKE FO customization layer -->
            <dependency>
                <groupId>network.ike.docs</groupId>
                <artifactId>docbook-xsl</artifactId>
                <version>${network.ike.docs__GA__ike-docs__VERSION}</version>
            </dependency>

            <!-- Koncept AsciiDoc extension -->
            <dependency>
                <groupId>network.ike.docs</groupId>
                <artifactId>koncept-asciidoc-extension</artifactId>
                <version>${network.ike.docs__GA__ike-docs__VERSION}</version>
            </dependency>

            <!-- ── AsciiDoc Toolchain ─────────────────────────────────── -->

            <dependency>
                <groupId>org.asciidoctor</groupId>
                <artifactId>asciidoctorj</artifactId>
                <version>${asciidoctorj.version}</version>
            </dependency>
            <dependency>
                <groupId>org.asciidoctor</groupId>
                <artifactId>asciidoctorj-pdf</artifactId>
                <version>${asciidoctorj-pdf.version}</version>
            </dependency>
            <dependency>
                <groupId>org.asciidoctor</groupId>
                <artifactId>asciidoctorj-diagram</artifactId>
                <version>${asciidoctorj-diagram.version}</version>
            </dependency>

            <!-- ── Test Dependencies ──────────────────────────────────── -->

            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter</artifactId>
                <version>${junit-jupiter.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.assertj</groupId>
                <artifactId>assertj-core</artifactId>
                <version>${assertj.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.mockito</groupId>
                <artifactId>mockito-core</artifactId>
                <version>${mockito.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.mockito</groupId>
                <artifactId>mockito-junit-jupiter</artifactId>
                <version>${mockito.version}</version>
                <scope>test</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <!-- ════════════════════════════════════════════════════════════════ -->
    <!-- Dependencies — inherited by external projects.                   -->
    <!-- ike-build-standards (from ike-tooling) is unpacked at validate     -->
    <!-- phase by the inherited unpack-claude-standards execution.          -->
    <!-- ════════════════════════════════════════════════════════════════ -->
    <dependencies>
        <dependency>
            <groupId>network.ike.tooling</groupId>
            <artifactId>ike-build-standards</artifactId>
            <classifier>claude</classifier>
            <type>zip</type>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>network.ike.tooling</groupId>
            <artifactId>ike-build-standards</artifactId>
            <classifier>config</classifier>
            <type>zip</type>
            <scope>provided</scope>
        </dependency>
        <!-- Scaffold templates (#243) — pulled at validate so
             ike:scaffold-{draft,publish,revert} have a sensible
             default scaffoldDir. -->
        <dependency>
            <groupId>network.ike.tooling</groupId>
            <artifactId>ike-build-standards</artifactId>
            <classifier>scaffold</classifier>
            <type>zip</type>
            <scope>provided</scope>
        </dependency>
        <!-- Unified logging (ike-issues#936): every inheriting module gets the
             SLF4J API on the compile path and the Log4j2 binding on the test
             path. Packaged apps supply the runtime binding via their jlink
             image, so it is test-scoped here. Versions come from the
             slf4j-bom / log4j-bom imports in dependencyManagement above. -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-slf4j2-impl</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <pluginManagement>
            <plugins>
                <plugin><artifactId>maven-clean-plugin</artifactId><version>3.4.0</version></plugin>
                <plugin><artifactId>maven-install-plugin</artifactId><version>3.1.3</version></plugin>
                <plugin><artifactId>maven-deploy-plugin</artifactId><version>3.1.3</version></plugin>

                <!-- Java 25 with preview features. External consumers       -->
                <!-- inheriting this POM (e.g. komet-desktop, doc-example)   -->
                <!-- need this here — the equivalent config in the           -->
                <!-- ike-platform reactor root only reaches modules that     -->
                <!-- parent to the reactor, not external ike-parent          -->
                <!-- consumers. See #280.                                    -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${maven-compiler-plugin.version}</version>
                    <!-- ASM in compiler plugin realm — symmetric with the   -->
                    <!-- surefire/failsafe injection in #282. Annotation     -->
                    <!-- processors (or maven-compiler-plugin internals)     -->
                    <!-- that read existing class files need a recent ASM    -->
                    <!-- to handle Java 25 class file major 69. See #283.    -->
                    <dependencies>
                        <dependency>
                            <groupId>org.ow2.asm</groupId>
                            <artifactId>asm</artifactId>
                            <version>${asm.version}</version>
                        </dependency>
                        <dependency>
                            <groupId>org.ow2.asm</groupId>
                            <artifactId>asm-tree</artifactId>
                            <version>${asm.version}</version>
                        </dependency>
                        <dependency>
                            <groupId>org.ow2.asm</groupId>
                            <artifactId>asm-commons</artifactId>
                            <version>${asm.version}</version>
                        </dependency>
                        <dependency>
                            <groupId>org.ow2.asm</groupId>
                            <artifactId>asm-util</artifactId>
                            <version>${asm.version}</version>
                        </dependency>
                    </dependencies>
                    <configuration>
                        <release>${java.version}</release>
                        <compilerArgs>
                            <arg>-parameters</arg>
                            <arg>--enable-preview</arg>
                        </compilerArgs>
                        <showWarnings>true</showWarnings>
                        <showDeprecation>true</showDeprecation>
                        <!-- Record-builder annotation processor — used      -->
                        <!-- across IKE Java modules. Promoted to ike-parent -->
                        <!-- so each consumer doesn't need to re-declare.    -->
                        <!-- See #282.                                       -->
                        <annotationProcessorPaths>
                            <annotationProcessorPath>
                                <groupId>io.soabase.record-builder</groupId>
                                <artifactId>record-builder-processor</artifactId>
                                <version>${record-builder-core.version}</version>
                            </annotationProcessorPath>
                        </annotationProcessorPaths>
                    </configuration>
                </plugin>

                <!-- Surefire/Failsafe argLines — preview features must be   -->
                <!-- enabled at test runtime to load preview-marked classes  -->
                <!-- (e.g. anything from tinkar-entity, komet-*).            -->
                <!-- @{argLine} preserves any agent argLine (jacoco etc.).   -->
                <!-- ASM dependencies in plugin realm — surefire 3.5.x's     -->
                <!-- bundled ASM doesn't reliably parse Java 25 class files  -->
                <!-- (major 69). Promoted from komet-desktop. See #282.      -->
                <!-- forkCount=1, reuseForks=false isolates each test JVM,   -->
                <!-- avoiding cross-test JVM state and giving stable timings -->
                <!-- under preview-features.                                  -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${maven-surefire-plugin.version}</version>
                    <dependencies>
                        <dependency>
                            <groupId>org.ow2.asm</groupId>
                            <artifactId>asm</artifactId>
                            <version>${asm.version}</version>
                        </dependency>
                        <dependency>
                            <groupId>org.ow2.asm</groupId>
                            <artifactId>asm-tree</artifactId>
                            <version>${asm.version}</version>
                        </dependency>
                        <dependency>
                            <groupId>org.ow2.asm</groupId>
                            <artifactId>asm-commons</artifactId>
                            <version>${asm.version}</version>
                        </dependency>
                    </dependencies>
                    <configuration>
                        <useModulePath>false</useModulePath>
                        <forkCount>1</forkCount>
                        <reuseForks>false</reuseForks>
                        <argLine>
                            @{argLine}
                            --enable-preview
                            --enable-native-access=ALL-UNNAMED
                            --add-opens java.base/java.lang=ALL-UNNAMED
                            --add-opens java.base/java.util=ALL-UNNAMED
                            --add-opens java.base/java.lang.reflect=ALL-UNNAMED
                            --add-opens java.base/java.io=ALL-UNNAMED
                        </argLine>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-failsafe-plugin</artifactId>
                    <version>${maven-failsafe-plugin.version}</version>
                    <dependencies>
                        <dependency>
                            <groupId>org.ow2.asm</groupId>
                            <artifactId>asm</artifactId>
                            <version>${asm.version}</version>
                        </dependency>
                        <dependency>
                            <groupId>org.ow2.asm</groupId>
                            <artifactId>asm-tree</artifactId>
                            <version>${asm.version}</version>
                        </dependency>
                        <dependency>
                            <groupId>org.ow2.asm</groupId>
                            <artifactId>asm-commons</artifactId>
                            <version>${asm.version}</version>
                        </dependency>
                    </dependencies>
                    <configuration>
                        <useModulePath>false</useModulePath>
                        <forkCount>1</forkCount>
                        <reuseForks>false</reuseForks>
                        <argLine>
                            @{failsafeArgLine}
                            --enable-preview
                            --enable-native-access=ALL-UNNAMED
                            --add-opens java.base/java.lang=ALL-UNNAMED
                            --add-opens java.base/java.util=ALL-UNNAMED
                            --add-opens java.base/java.lang.reflect=ALL-UNNAMED
                            --add-opens java.base/java.io=ALL-UNNAMED
                        </argLine>
                    </configuration>
                </plugin>

                <!-- maven-jar-plugin — manifest entries used by             -->
                <!-- WorkspaceReport.pluginVersion to discover plugin        -->
                <!-- version at runtime (see #208). Promoted from reactor    -->
                <!-- root so external consumers get it. See #282.            -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-jar-plugin</artifactId>
                    <version>${maven-jar-plugin.version}</version>
                    <configuration>
                        <archive>
                            <manifest>
                                <addDefaultImplementationEntries>true</addDefaultImplementationEntries>
                                <addDefaultSpecificationEntries>true</addDefaultSpecificationEntries>
                            </manifest>
                        </archive>
                    </configuration>
                </plugin>

                <!-- maven-source-plugin — auto-attach sources jar to every  -->
                <!-- artifact. Promoted from reactor root so external        -->
                <!-- consumers publish sources without re-declaring. #282.   -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-source-plugin</artifactId>
                    <version>${maven-source-plugin.version}</version>
                    <executions>
                        <execution>
                            <id>attach-sources</id>
                            <goals>
                                <goal>jar-no-fork</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>

                <!-- jacoco — code coverage. prepare-agent (surefire) and    -->
                <!-- prepare-agent-integration (failsafe) symmetrically set  -->
                <!-- @{argLine} and @{failsafeArgLine}, which the parent     -->
                <!-- argLine configs above reference. Skip with              -->
                <!-- -Dike.skip.jacoco=true. See #282.                       -->
                <plugin>
                    <groupId>org.jacoco</groupId>
                    <artifactId>jacoco-maven-plugin</artifactId>
                    <version>${jacoco-maven-plugin.version}</version>
                    <configuration>
                        <skip>${ike.skip.jacoco}</skip>
                        <formats>
                            <format>HTML</format>
                            <format>XML</format>
                            <format>CSV</format>
                        </formats>
                    </configuration>
                    <executions>
                        <execution>
                            <id>jacoco-prepare-agent</id>
                            <goals><goal>prepare-agent</goal></goals>
                        </execution>
                        <execution>
                            <id>jacoco-prepare-agent-integration</id>
                            <goals><goal>prepare-agent-integration</goal></goals>
                        </execution>
                        <execution>
                            <id>jacoco-report</id>
                            <phase>verify</phase>
                            <goals><goal>report</goal></goals>
                        </execution>
                        <execution>
                            <id>jacoco-report-integration</id>
                            <phase>verify</phase>
                            <goals><goal>report-integration</goal></goals>
                        </execution>
                    </executions>
                </plugin>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-javadoc-plugin</artifactId>
                    <configuration>
                        <doclint>${ike.javadoc.doclint}</doclint>
                        <failOnWarnings>${ike.javadoc.failOnWarnings}</failOnWarnings>
                        <source>${java.version}</source>
                        <additionalOptions>
                            <additionalOption>--enable-preview</additionalOption>
                        </additionalOptions>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-artifact-plugin</artifactId>
                    <version>3.5.2</version>
                    <executions>
                        <execution>
                            <id>buildinfo</id>
                            <goals><goal>buildinfo</goal></goals>
                            <phase>verify</phase>
                        </execution>
                    </executions>
                </plugin>

                <!-- Maven Dependency Plugin — unpack-site-theme execution
                     config (#318, #330). External consumers inheriting
                     this POM (ike-lab-documents, doc-example,
                     project-example, integration-tests-example)
                     activate this execution via the site-resources
                     profile. The equivalent config previously lived only
                     in the ike-platform reactor root pluginManagement,
                     which external consumers do not inherit; symptom:
                     every external consumer's site rendered with the
                     default Sentry skin placeholder instead of the
                     canonical Forest theme. Mirrors the same fix
                     pattern as the maven-compiler-plugin lift in #280.

                     Source moved from ike-build-standards:site-theme
                     (Tier 1) to ike-base-parent:site-theme (Tier 0) in
                     #464 so direct ike-base-parent inheritors get the
                     theme natively and Tier 0 stops depending on
                     Tier 1. -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-dependency-plugin</artifactId>
                    <version>${maven-dependency-plugin.version}</version>
                    <executions>
                        <execution>
                            <id>unpack-site-theme</id>
                            <phase>pre-site</phase>
                            <goals><goal>unpack-dependencies</goal></goals>
                            <configuration>
                                <includeGroupIds>network.ike</includeGroupIds>
                                <includeArtifactIds>ike-base-parent</includeArtifactIds>
                                <includeClassifiers>site-theme</includeClassifiers>
                                <includeTypes>zip</includeTypes>
                                <outputDirectory>${project.build.directory}/generated-site/resources</outputDirectory>
                                <overWriteReleases>true</overWriteReleases>
                                <overWriteSnapshots>true</overWriteSnapshots>
                            </configuration>
                        </execution>
                        <!-- Built-With supplement (ike-issues#340).
                             Unpacks the platform-wide supplement.yaml
                             from ike-build-standards:built-with:zip
                             at pre-site phase to
                             target/built-with-supplement.yaml. The
                             ike:built-with mojo reads it as a
                             third-priority fallback after the
                             per-project and walk-up locations, so
                             external consumers (ike-lab-documents,
                             doc-example, example-project, etc.) get
                             the Curated narrative section on their
                             built-with.html page without authoring
                             their own supplement. -->
                        <execution>
                            <id>unpack-built-with-supplement</id>
                            <phase>initialize</phase>
                            <goals><goal>unpack-dependencies</goal></goals>
                            <configuration>
                                <includeArtifactIds>ike-build-standards</includeArtifactIds>
                                <includeClassifiers>built-with</includeClassifiers>
                                <includeTypes>zip</includeTypes>
                                <outputDirectory>${project.build.directory}</outputDirectory>
                                <overWriteReleases>true</overWriteReleases>
                                <overWriteSnapshots>true</overWriteSnapshots>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>

                <!-- ── maven-resources-plugin ──────────────────────────────
                     Hosts the copy-bom-to-site execution (#333) so the
                     CycloneDX SBOM produced at package phase is reachable
                     from the rendered Maven Site at site:site time. The
                     resources plugin runs once at pre-site, copying
                     target/bom.{json,xml} into target/site/ where Maven
                     Site picks them up alongside the rendered HTML. -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-resources-plugin</artifactId>
                    <version>${maven-resources-plugin.version}</version>
                    <executions>
                        <execution>
                            <id>copy-bom-to-site</id>
                            <phase>pre-site</phase>
                            <goals><goal>copy-resources</goal></goals>
                            <configuration>
                                <outputDirectory>${project.build.directory}/site</outputDirectory>
                                <resources>
                                    <resource>
                                        <directory>${project.build.directory}</directory>
                                        <includes>
                                            <include>bom.json</include>
                                            <include>bom.xml</include>
                                        </includes>
                                    </resource>
                                </resources>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>

                <!-- ── cyclonedx-maven-plugin ──────────────────────────────
                     Generates a CycloneDX Software Bill of Materials at
                     package phase: target/bom.json + target/bom.xml.
                     Auto-attaches them as Maven artifacts with classifier
                     `cyclonedx`, so consumers can pull the SBOM through
                     Nexus / Maven Central as well as from the published
                     site (see copy-bom-to-site above).

                     Licenses are normalized to SPDX expressions
                     (`Apache-2.0`, `EPL-2.0 OR GPL-2.0-only`, …) so the
                     output is ingestible by Dependency-Track, Trivy,
                     Snyk, and GitHub's dependency graph without the
                     POM-string noise that plagues licenses.html. See
                     ike-issues#333. -->
                <plugin>
                    <groupId>org.cyclonedx</groupId>
                    <artifactId>cyclonedx-maven-plugin</artifactId>
                    <version>2.9.0</version>
                    <executions>
                        <execution>
                            <id>make-bom</id>
                            <phase>package</phase>
                            <goals><goal>makeBom</goal></goals>
                        </execution>
                    </executions>
                </plugin>

                <!-- maven-site-plugin configuration centralized here so
                     every consumer inheriting ike-parent gets the same
                     site descriptor behavior. ike-issues#360.

                     relativizeSiteLinks=false: the default (true) rewrites
                     absolute URLs in site.xml (banner hrefs, breadcrumbs,
                     menu links, the Issues link) against project.url.
                     For cross-host targets (github.com) and cross-repo
                     targets (ike.network/<sibling>/) that rebase produces
                     broken paths — e.g. github.com/IKE-Network/ike-issues
                     becomes ../ike-issues which resolves to
                     https://ike.network/ike-issues (404). Setting false
                     keeps absolute URLs intact across the whole
                     IKE-Network multi-repo layout. -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-site-plugin</artifactId>
                    <configuration>
                        <relativizeSiteLinks>false</relativizeSiteLinks>
                        <!-- topSiteURL pins site:stage's relative-path
                             math to the org root so the staging
                             destination resolves under target/staging
                             instead of escaping out via ../ segments.

                             Without this, site:stage computes
                             destination = relative(topSiteURL, siteURL)
                             with topSiteURL defaulting to project.url
                             (which has a /ike-platform/ike-parent/
                             prefix inherited from this pom's own
                             <url>). The relative math produces
                             ../../<artifactId>/ which, resolved
                             against target/staging/, lands at
                             <basedir>/<artifactId>/ — the
                             ike-issues#358 on-disk gh-pages leak.

                             Pinning topSiteURL to the org root makes
                             the relative path just <artifactId>/, so
                             site:stage stages at
                             target/staging/<artifactId>/ as intended. -->
                        <topSiteURL>https://ike.network/</topSiteURL>
                    </configuration>
                </plugin>

                <!-- ═══════════════════════════════════════════════════════
                     IKE plugin version pins (#524)
                     ═══════════════════════════════════════════════════════

                     Three IKE-authored plugins. Version pins live here
                     in pluginManagement (declarative, propagates to
                     descendants). Active <plugins> entries below carry
                     only executions, no <version> — version inherits
                     from pluginManagement.

                     Earlier revisions placed both version pin and
                     executions in active <plugins> with a now-retired
                     <extensions>true</extensions> marker forcing that
                     location for the ike-doc packaging type. That
                     custom packaging was retired in #321 in favor of
                     a classifier-canonical doc shape; the location
                     vestige is cleaned up here. -->

                <!-- IKE Maven Plugin — release orchestration, BOM
                     generation, site deploy, AsciiDoc utilities.
                     External (from ike-tooling). Executions activate
                     in active <plugins> below. -->
                <plugin>
                    <groupId>network.ike.tooling</groupId>
                    <artifactId>ike-maven-plugin</artifactId>
                    <version>${network.ike.tooling__GA__ike-tooling__VERSION}</version>
                </plugin>

                <!-- IKE Doc Maven Plugin — idoc:* render goals
                     (AsciiDoc, multi-renderer PDF wrappers). External
                     (from ike-docs). Executions activate in active
                     <plugins> below. -->
                <plugin>
                    <groupId>network.ike.docs</groupId>
                    <artifactId>ike-doc-maven-plugin</artifactId>
                    <version>${network.ike.docs__GA__ike-docs__VERSION}</version>
                </plugin>

                <!-- IKE Workspace Maven Plugin — workspace management
                     goals (ws:*). Co-released with ike-parent in this
                     reactor. Inheriting projects pick up ws: goals at
                     the matching version. A bare-GAV entry in active
                     <plugins> registers the ws: prefix for the
                     project; version resolves from this pluginManagement
                     entry. -->
                <plugin>
                    <groupId>network.ike.platform</groupId>
                    <artifactId>ike-workspace-maven-plugin</artifactId>
                    <version>148</version>
                </plugin>
            </plugins>
        </pluginManagement>

        <!-- Activate standard unpacks for all inheriting projects.
             unpack-dependencies resolves from reactor; no-op if
             the dependency is absent. -->
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <executions>
                    <execution>
                        <id>unpack-claude-standards</id>
                        <!-- inherits config from pluginManagement -->
                    </execution>
                    <execution>
                        <id>unpack-build-tools</id>
                        <!-- inherits config from pluginManagement -->
                    </execution>
                    <execution>
                        <id>unpack-scaffold-templates</id>
                        <!-- inherits config from pluginManagement
                             (#243): unpacks ike-build-standards
                             classifier=scaffold to ${project.build.directory}/scaffold
                             at validate phase, so ike:scaffold-*
                             goals work in prefix form without a
                             mandatory -DscaffoldDir flag. -->
                    </execution>
                </executions>
            </plugin>

            <!-- JaCoCo — code coverage for all inheriting projects.
                 Configuration is inherited from root POM pluginManagement.
                 Skip with -Dike.skip.jacoco=true. Reports in target/site/jacoco/ -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
            </plugin>

            <!-- SBOM (#333) — CycloneDX produces target/bom.{json,xml}
                 at package phase; maven-resources-plugin copies them
                 into target/site/ at pre-site so they're reachable
                 from the published Maven Site. Configuration in
                 pluginManagement above. -->
            <plugin>
                <groupId>org.cyclonedx</groupId>
                <artifactId>cyclonedx-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <executions>
                    <execution>
                        <id>copy-bom-to-site</id>
                        <!-- inherits config from pluginManagement -->
                    </execution>
                </executions>
            </plugin>

            <!-- IKE Maven Plugin — release orchestration, BOM generation,
                 site deploy, AsciiDoc utilities. External (from
                 ike-tooling). Version pin lives in pluginManagement
                 above (#524); this entry only binds executions. -->
            <plugin>
                <groupId>network.ike.tooling</groupId>
                <artifactId>ike-maven-plugin</artifactId>
                <executions>
                    <!-- SPDX-grouped licenses.adoc generation
                         (ike-issues#335). Reads target/bom.json at
                         pre-site and writes target/generated-site/
                         asciidoc/licenses.adoc; Maven Site renders
                         to target/site/licenses.html with the
                         project skin chrome. The auto-generated
                         `licenses` report from
                         maven-project-info-reports is dropped from
                         this POM's reportSet so the SPDX-grouped
                         page is the only licenses.html that ships.
                         External consumers inheriting ike-parent
                         get the same wiring automatically. Skip
                         with -Dike.skip.spdx-licenses=true. -->
                    <execution>
                        <id>render-spdx-licenses</id>
                        <phase>pre-site</phase>
                        <goals><goal>render-spdx-licenses</goal></goals>
                    </execution>
                    <!-- Built With page (ike-issues#336). Reads
                         target/bom.json plus the reactor-root
                         src/main/built-with/supplement.yaml and
                         writes target/generated-site/asciidoc/
                         built-with.adoc. Maven Site renders to
                         target/site/built-with.html. External
                         consumers inheriting ike-parent get the
                         same wiring automatically. Skip with
                         -Dike.skip.built-with=true. -->
                    <execution>
                        <id>built-with</id>
                        <phase>pre-site</phase>
                        <goals><goal>built-with</goal></goals>
                    </execution>
                    <!-- SBOM viewer page (ike-issues#341). Reads
                         target/bom.json and writes target/
                         generated-site/asciidoc/dependencies.adoc
                         — a sortable Web-friendly view of the full
                         transitive dependency graph. Maven Site
                         renders to dependencies.html. Replaces the
                         auto-generated `dependencies` report
                         (dropped from this POM's reportSet). Skip
                         with -Dike.skip.sbom-viewer=true. -->
                    <execution>
                        <id>render-sbom-viewer</id>
                        <phase>pre-site</phase>
                        <goals><goal>render-sbom-viewer</goal></goals>
                    </execution>
                </executions>
            </plugin>

            <!-- IKE Doc Maven Plugin — idoc:* render goals
                 (AsciiDoc, multi-renderer PDF wrappers). External
                 (from ike-docs). Version pin lives in pluginManagement
                 above (#524); this entry only binds executions. -->
            <plugin>
                <groupId>network.ike.docs</groupId>
                <artifactId>ike-doc-maven-plugin</artifactId>
                <executions>
                    <!-- site.xml linter (#319). Runs at validate phase
                         on every project that inherits ike-parent and
                         has src/site/site.xml. Silent no-op when the
                         file doesn't exist (modules without their own
                         site descriptor). Catches drift in <bodyClass>
                         (Forest vs Horizon, post-#307), stale
                         breadcrumb names (IKE Pipeline, post-#216),
                         and skin GAV. Skip with
                         -Dike.skip.lint-site=true. -->
                    <execution>
                        <id>lint-site</id>
                        <phase>validate</phase>
                        <goals><goal>lint-site</goal></goals>
                    </execution>
                </executions>
            </plugin>

            <!-- IKE Workspace Maven Plugin — workspace management goals
                 (ws:*). Bare-GAV entry: no version (inherits from
                 pluginManagement above, #524), no executions. Presence
                 in active <plugins> registers the ws: prefix for the
                 project so inheriting projects can invoke ws:* goals
                 by prefix without specifying full GAV. -->
            <plugin>
                <groupId>network.ike.platform</groupId>
                <artifactId>ike-workspace-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

    <!-- ════════════════════════════════════════════════════════════════ -->
    <!-- Profiles                                                        -->
    <!-- ════════════════════════════════════════════════════════════════ -->
    <profiles>
        <!-- ── doc-pipeline ─────────────────────────────────────────── -->
        <!-- Activated when src/docs/asciidoc exists. Provides doc       -->
        <!-- artifact dependencies and their unpack executions.          -->
        <!-- The rest of the doc pipeline (asciidoctor, resources,       -->
        <!-- assembly) is inherited from the root POM’s doc-pipeline     -->
        <!-- profile.                                                    -->
        <profile>
            <id>doc-pipeline-deps</id>
            <activation>
                <file><exists>src/docs/asciidoc</exists></file>
            </activation>

            <dependencies>
                <dependency>
                    <groupId>network.ike.tooling</groupId>
                    <artifactId>ike-build-standards</artifactId>
                    <classifier>asciidoctorconfig</classifier>
                    <type>zip</type>
                    <scope>provided</scope>
                </dependency>
                <dependency>
                    <groupId>network.ike.docs</groupId>
                    <artifactId>ike-doc-resources</artifactId>
                    <scope>provided</scope>
                </dependency>
                <dependency>
                    <groupId>network.ike.docs</groupId>
                    <artifactId>minimal-fonts</artifactId>
                    <type>zip</type>
                </dependency>
                <dependency>
                    <groupId>network.ike.docs</groupId>
                    <artifactId>docbook-xsl</artifactId>
                    <scope>provided</scope>
                </dependency>
            </dependencies>

            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-dependency-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>unpack-asciidoctorconfig</id>
                                <!-- inherits config from pluginManagement -->
                            </execution>
                            <execution>
                                <id>unpack-doc-resources</id>
                                <!-- inherits config from pluginManagement -->
                            </execution>
                            <execution>
                                <id>unpack-doc-dependencies</id>
                                <!-- inherits config from pluginManagement —
                                     unpacks the topic-library adoc zip so
                                     assembly include::{topics}/... resolves
                                     in a Maven build. IKE-Network/ike-issues#502 -->
                            </execution>
                            <execution>
                                <id>unpack-fonts</id>
                                <!-- inherits config from pluginManagement -->
                            </execution>
                            <execution>
                                <id>unpack-docbook-xsl</id>
                                <!-- inherits config from pluginManagement -->
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <!-- ── site-resources ───────────────────────────────────────── -->
        <!-- Activated when src/site exists. Pulls the canonical Forest -->
        <!-- theme site.css + ike-logo.svg from ike-base-parent          -->
        <!-- (classifier=site-theme) and unpacks them at pre-site into  -->
        <!-- target/generated-site/resources/, which Maven Site merges  -->
        <!-- into target/site at site:site time. See #318, #464.        -->
        <!-- Moved from ike-build-standards:site-theme (Tier 1) to       -->
        <!-- ike-base-parent:site-theme (Tier 0) per #464 so direct      -->
        <!-- ike-base-parent inheritors (ike-workspace-extension) get    -->
        <!-- the theme natively, and so Tier 0 stops depending on Tier 1.-->
        <profile>
            <id>site-resources</id>
            <activation>
                <file><exists>src/site</exists></file>
            </activation>

            <dependencies>
                <dependency>
                    <groupId>network.ike</groupId>
                    <artifactId>ike-base-parent</artifactId>
                    <version>${ike-base-parent.version}</version>
                    <classifier>site-theme</classifier>
                    <type>zip</type>
                    <scope>provided</scope>
                </dependency>
                <!-- Built-With supplement (ike-issues#340). Pulls
                     the platform-wide supplement.yaml so the
                     unpack-built-with-supplement execution above
                     can drop it at target/built-with-supplement.yaml
                     for the ike:built-with mojo's third-priority
                     fallback. -->
                <dependency>
                    <groupId>network.ike.tooling</groupId>
                    <artifactId>ike-build-standards</artifactId>
                    <classifier>built-with</classifier>
                    <type>zip</type>
                    <scope>provided</scope>
                </dependency>
            </dependencies>

            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-dependency-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>unpack-site-theme</id>
                                <!-- inherits config from pluginManagement -->
                            </execution>
                            <execution>
                                <id>unpack-built-with-supplement</id>
                                <!-- inherits config from pluginManagement -->
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <!-- ── skip-javadoc-for-pom ─────────────────────────────────── -->
        <!-- POM-packaging modules have no Java source. Skip javadoc     -->
        <!-- and source jar to avoid "fake javadoc directory" warnings.   -->
        <profile>
            <id>skip-javadoc-for-pom</id>
            <!-- Activated on modules with no Java source. Was a
                 <packaging>pom</packaging> activation, but that is a
                 Maven 4.1-only construct that blocks the model-4.0.0
                 consumer-POM transform (and thus Maven Central). The
                 file-missing form is 4.0.0-compatible and matches the
                 intent: no Java source. IKE-Network/ike-issues#445. -->
            <activation>
                <file>
                    <missing>src/main/java</missing>
                </file>
            </activation>
            <properties>
                <maven.javadoc.skip>true</maven.javadoc.skip>
                <maven.source.skip>true</maven.source.skip>
            </properties>
        </profile>
    </profiles>
</project>
